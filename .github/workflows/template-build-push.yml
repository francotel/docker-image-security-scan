name: secure-docker-pipeline-template

on:
  workflow_call:
    inputs:
      # Application configuration
      image:
        type: string
        required: true
      
      dockerfile_path:
        type: string
        required: true
        description: "Path to the Dockerfile"
      
      target_env:
        type: string
        required: true
        description: "Target environment (dev, stg, prd)"
      
      aws_ecr_registry:
        type: string
        required: true
        description: "AWS ECR registry URL"
    
    secrets:
      # AWS credentials
      AWS_ACCESS_KEY_ID:
        required: true
      AWS_SECRET_ACCESS_KEY:
        required: true
      AWS_REGION:
        required: true

env:
  # Security scanning configuration
  TRIVY_VERSION: "0.51.1"
  CHECKOV_VERSION: "3.2.497"
  SCAN_TIMEOUT_MINUTES: 10
  FAIL_ON_CRITICAL: true
  FAIL_ON_HIGH: true

jobs:
  build_scan_and_push:
    name: 🛡️ Build, Scan & Push
    runs-on: ubuntu-latest
    environment: ${{ inputs.target_env }}

    steps:
      # ============ INITIAL SETUP ============
      - name: 📂 Checkout repository
        uses: actions/checkout@v4

      - name: 🔑 Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: 🔐 Login to AWS ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      # # ============ PREPARACIÓN DE TAGS ============
      # - name: stp-PrepareImageTags
      #   id: prep-tags
      #   run: |
      #     SHORT_SHA=${GITHUB_SHA::7}
      #     echo "IMAGE_TAG_SHA=sha-${SHORT_SHA}" >> $GITHUB_ENV
      #     echo "IMAGE_TAG_ENV=${{ inputs.target_env }}-latest" >> $GITHUB_ENV

      # # ============ CONSTRUCCIÓN DE IMAGEN ============
      # - name: stp-BuildDockerImage
      #   id: stp-BuildDockerImage
      #   uses: docker/build-push-action@v5
      #   with:
      #     file: docker/${{ inputs.image }}/Dockerfile
      #     push: false
      #     no-cache: true
      #     tags: |
      #       ${{ inputs.acr_name }}.azurecr.io/nginx-${{ inputs.image }}:${{ env.IMAGE_TAG_SHA }}
      #       ${{ inputs.acr_name }}.azurecr.io/nginx-${{ inputs.image }}:${{ env.IMAGE_TAG_ENV }}

      # # ============ ESCANEO DE SEGURIDAD - TRIVY ============
      # - name: stp-TrivyImageScan
      #   run: |
      #     echo "🔎 Escaneando imagen con Trivy..."
      #     mkdir -p "${{ github.workspace }}/trivy-report"

      #     docker run --rm \
      #       -v ${{ github.workspace }}:/src \
      #       -v /var/run/docker.sock:/var/run/docker.sock \
      #       -v ${{ github.workspace }}/config/trivy:/config \
      #       -v "${{ github.workspace }}/trivy-report:/reports" \
      #       aquasec/trivy:${{ env.TRIVY_VERSION }} \
      #       image \
      #       --exit-code ${{ env.TRIVY_EXIT_CODE }} \
      #       --config /config/trivy.yaml \
      #       --format json \
      #       --output /src/trivy-report/trivy-results.json \
      #       ${{ inputs.acr_name }}.azurecr.io/nginx-${{ inputs.image }}:${{ env.IMAGE_TAG_SHA }}

      # - name: stp-SummaryVulnerabilities
      #   working-directory: ./trivy-report
      #   run: |
      #     echo "## 🐳 Reporte de Escaneo - Trivy" >> $GITHUB_STEP_SUMMARY
      #     echo "" >> $GITHUB_STEP_SUMMARY

      #     if [ -f trivy-results.json ]; then
      #       TOTAL=$(jq '[.Results[] | (.Vulnerabilities // []), (.Misconfigurations // []), (.Secrets // [])] | flatten | length' trivy-results.json || echo 0)
      #       CRIT=$(jq '[.Results[]?.Vulnerabilities[]? | select(.Severity=="CRITICAL")] | length' trivy-results.json || echo 0)
      #       HIGH=$(jq '[.Results[]?.Vulnerabilities[]? | select(.Severity=="HIGH")] | length' trivy-results.json || echo 0)
      #       MED=$(jq '[.Results[]?.Vulnerabilities[]? | select(.Severity=="MEDIUM")] | length' trivy-results.json || echo 0)
      #       LOW=$(jq '[.Results[]?.Vulnerabilities[]? | select(.Severity=="LOW")] | length' trivy-results.json || echo 0)
      #       MIS=$(jq '[.Results[]?.Misconfigurations[]?] | length' trivy-results.json || echo 0)
      #       SECRETS=$(jq '[.Results[]?.Secrets[]?] | length' trivy-results.json || echo 0)

      #       echo "### 📊 Resumen General" >> $GITHUB_STEP_SUMMARY
      #       echo "" >> $GITHUB_STEP_SUMMARY
      #       echo "| Tipo | Severidad | Cantidad |" >> $GITHUB_STEP_SUMMARY
      #       echo "|------|-----------|----------|" >> $GITHUB_STEP_SUMMARY
      #       [ "$CRIT" -gt 0 ] && echo "| Vulnerabilidades | 🔴 CRÍTICA | $CRIT |" >> $GITHUB_STEP_SUMMARY
      #       [ "$HIGH" -gt 0 ] && echo "| Vulnerabilidades | 🟠 ALTA | $HIGH |" >> $GITHUB_STEP_SUMMARY
      #       [ "$MED" -gt 0 ] && echo "| Vulnerabilidades | 🟡 MEDIA | $MED |" >> $GITHUB_STEP_SUMMARY
      #       [ "$LOW" -gt 0 ] && echo "| Vulnerabilidades | 🔵 BAJA | $LOW |" >> $GITHUB_STEP_SUMMARY
      #       [ "$MIS" -gt 0 ] && echo "| Configuraciones | - | $MIS |" >> $GITHUB_STEP_SUMMARY
      #       [ "$SECRETS" -gt 0 ] && echo "| Secretos | - | $SECRETS |" >> $GITHUB_STEP_SUMMARY
      #       echo "| **Total** | - | **$TOTAL** |" >> $GITHUB_STEP_SUMMARY
      #       echo "" >> $GITHUB_STEP_SUMMARY

      #       echo "### 📋 Metadatos" >> $GITHUB_STEP_SUMMARY
      #       echo "- **Fecha del escaneo:** $(date -u '+%Y-%m-%d %H:%M:%S UTC')" >> $GITHUB_STEP_SUMMARY
      #       echo "- **Rama:** ${{ github.head_ref || github.ref_name }}" >> $GITHUB_STEP_SUMMARY
      #       echo "- **Repositorio:** ${{ github.repository }}" >> $GITHUB_STEP_SUMMARY

      #       if [ "$TOTAL" -eq 0 ]; then
      #         echo "" >> $GITHUB_STEP_SUMMARY
      #         echo "✅ **No se encontraron vulnerabilidades.**" >> $GITHUB_STEP_SUMMARY
      #       fi
      #     else
      #       echo "⚠️ **No se encontró el archivo de resultados de Trivy**" >> $GITHUB_STEP_SUMMARY
      #     fi

      # - name: stp-ListVulnerabilities
      #   working-directory: ./trivy-report
      #   run: |
      #     echo "### 🔍 Detalle de Vulnerabilidades" >> $GITHUB_STEP_SUMMARY
      #     echo "" >> $GITHUB_STEP_SUMMARY
          
      #     if [ -f trivy-results.json ]; then
      #       # Vulnerabilidades CRÍTICAS
      #       CRITICAL_COUNT=$(jq '[.Results[]?.Vulnerabilities[]? | select(.Severity=="CRITICAL")] | length' trivy-results.json || echo 0)
      #       if [ "$CRITICAL_COUNT" -gt 0 ]; then
      #         echo "#### 🔴 Vulnerabilidades Críticas ($CRITICAL_COUNT)" >> $GITHUB_STEP_SUMMARY
      #         echo "" >> $GITHUB_STEP_SUMMARY
      #         echo "| CVE | Paquete | Versión | Versión Corregida |" >> $GITHUB_STEP_SUMMARY
      #         echo "|-----|---------|---------|-------------------|" >> $GITHUB_STEP_SUMMARY
      #         jq -r '.Results[]?.Vulnerabilities[]? | select(.Severity=="CRITICAL") | "| \(.VulnerabilityID) | `\(.PkgName)` | `\(.InstalledVersion)` | `\(.FixedVersion // "No disponible")` |"' trivy-results.json 2>/dev/null | head -15 >> $GITHUB_STEP_SUMMARY
      #         echo "" >> $GITHUB_STEP_SUMMARY
      #       fi
            
      #       # Vulnerabilidades ALTAS
      #       HIGH_COUNT=$(jq '[.Results[]?.Vulnerabilities[]? | select(.Severity=="HIGH")] | length' trivy-results.json || echo 0)
      #       if [ "$HIGH_COUNT" -gt 0 ]; then
      #         echo "#### 🟠 Vulnerabilidades Altas ($HIGH_COUNT)" >> $GITHUB_STEP_SUMMARY
      #         echo "" >> $GITHUB_STEP_SUMMARY
      #         echo "| CVE | Paquete | Versión | Versión Corregida |" >> $GITHUB_STEP_SUMMARY
      #         echo "|-----|---------|---------|-------------------|" >> $GITHUB_STEP_SUMMARY
      #         jq -r '.Results[]?.Vulnerabilities[]? | select(.Severity=="HIGH") | "| \(.VulnerabilityID) | `\(.PkgName)` | `\(.InstalledVersion)` | `\(.FixedVersion // "No disponible")` |"' trivy-results.json 2>/dev/null | head -10 >> $GITHUB_STEP_SUMMARY
      #         echo "" >> $GITHUB_STEP_SUMMARY
      #       fi
            
      #       if [ "$CRITICAL_COUNT" -eq 0 ] && [ "$HIGH_COUNT" -eq 0 ]; then
      #         echo "✅ No se encontraron vulnerabilidades críticas o altas." >> $GITHUB_STEP_SUMMARY
      #       fi
            
      #       # Estadísticas adicionales
      #       echo "#### 📈 Estadísticas" >> $GITHUB_STEP_SUMMARY
      #       echo "- **Total de CVEs:** $(jq '[.Results[]?.Vulnerabilities[]? | .VulnerabilityID] | length' trivy-results.json 2>/dev/null || echo 0)" >> $GITHUB_STEP_SUMMARY
      #       echo "- **Paquetes afectados:** $(jq '.Results[]?.Vulnerabilities[]? | .PkgName' trivy-results.json 2>/dev/null | sort -u | wc -l)" >> $GITHUB_STEP_SUMMARY
      #       echo "- **Correcciones disponibles:** $(jq '[.Results[]?.Vulnerabilities[]? | select(.FixedVersion != null)] | length' trivy-results.json 2>/dev/null || echo 0)" >> $GITHUB_STEP_SUMMARY
      #     else
      #       echo "⚠️ No se encontró el archivo de resultados" >> $GITHUB_STEP_SUMMARY
      #     fi

      # # ============ ESCANEO DE SEGURIDAD - CHECKOV ============
      # - name: stp-CheckovScan
      #   run: |
      #     echo "🔎 Escaneando Dockerfile con Checkov..."

      #     docker run --rm --user "$(id -u):$(id -g)" \
      #       -v ${{ github.workspace }}:/src \
      #       -v ${{ github.workspace }}/config/checkov:/config/ \
      #       bridgecrew/checkov:${{ env.CHECKOV_VERSION }} \
      #       --file /src/docker/${{ inputs.image }}/Dockerfile \
      #       --framework dockerfile \
      #       --config-file /config/checkov.yaml \
      #       --output github_failed_only \
      #       --output-file /src/checkov-report

      # - name: stp-CheckovReportSummary
      #   id: checkov-report
      #   run: |
      #     echo "---" >> $GITHUB_STEP_SUMMARY
      #     echo "## 🏗️ Reporte de Escaneo - Checkov" >> $GITHUB_STEP_SUMMARY
      #     echo "" >> $GITHUB_STEP_SUMMARY
          
      #     if [ -f checkov-report/results_github_failed_only.md ]; then
      #       if grep -q "Failed" checkov-report/results_github_failed_only.md; then
      #         cat checkov-report/results_github_failed_only.md >> $GITHUB_STEP_SUMMARY
      #       else
      #         echo "✅ Sin problemas detectados" >> $GITHUB_STEP_SUMMARY
      #       fi
      #     fi

      # # ============ LIMPIEZA DE RECURSOS ============
      # - name: stp-CleanResources
      #   if: always()
      #   run: |
      #     echo "🧹 Limpiando archivos temporales..."
      #     sudo rm -rf "${{ github.workspace }}/checkov-report" 2>/dev/null || true
      #     sudo rm -rf "${{ github.workspace }}/trivy-report" 2>/dev/null || true

      # - name: stp-CleanUpImages
      #   working-directory: ./docker/${{ inputs.image }}
      #   run: |
      #     echo "🗑️ Eliminando imágenes locales..."
      #     docker rmi -f ${{ inputs.acr_name }}.azurecr.io/nginx-${{ inputs.image }}:${{ env.IMAGE_TAG_SHA }} 2>/dev/null || echo "⚠️ No se pudo eliminar la imagen sha"
      #     docker rmi -f ${{ inputs.acr_name }}.azurecr.io/nginx-${{ inputs.image }}:${{ env.IMAGE_TAG_ENV }} 2>/dev/null || echo "⚠️ No se pudo eliminar la imagen latest"

      # # ============ VALIDACIÓN FINAL ============
      # - name: stp-BlockSummaryFailures
      #   run: |
      #     echo "🔒 Validación Final de Seguridad"
      #     echo "================================"
          
      #     URL_SUMMARY="https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}#summary"
          
      #     # Contar problemas de Trivy
      #     PROBLEMAS_TRIVY=0
      #     if [ -f trivy-report/trivy-results.json ]; then
      #       PROBLEMAS_TRIVY=$(jq '[.Results[].Vulnerabilities[]? | select(.Severity=="CRITICAL" or .Severity=="HIGH")] | length' ./trivy-report/trivy-results.json 2>/dev/null || echo 0)
      #     fi
          
      #     # Contar problemas de Checkov
      #     PROBLEMAS_CHECKOV=0
      #     if [ -f checkov-report/results_github_failed_only.md ]; then
      #       FALLIDOS=$(grep -o "Failed Checks: [0-9]*" checkov-report/results_github_failed_only.md | head -1 | grep -o "[0-9]*" || echo 0)
      #       PROBLEMAS_CHECKOV=$FALLIDOS
      #     fi
          
      #     TOTAL_PROBLEMAS=$((PROBLEMAS_TRIVY + PROBLEMAS_CHECKOV))
          
      #     if [ "$TOTAL_PROBLEMAS" -gt 0 ]; then
      #       echo ""
      #       echo "❌ **WORKFLOW FALLIDO**"
      #       echo ""
      #       echo "**Problemas detectados:**"
      #       echo ""
      #       [ "$PROBLEMAS_TRIVY" -gt 0 ] && echo "- 🐳 **Trivy:** $PROBLEMAS_TRIVY vulnerabilidades críticas/altas"
      #       [ "$PROBLEMAS_CHECKOV" -gt 0 ] && echo "- 🏗️ **Checkov:** $PROBLEMAS_CHECKOV checks fallidos"
      #       echo ""
      #       echo "**Acciones requeridas:**"
      #       echo "1. 📋 Revisa el reporte completo: $URL_SUMMARY"
      #       echo "2. 🔧 Corrige los problemas identificados"
      #       echo "3. 🔄 Vuelve a ejecutar el workflow"
      #       echo ""
      #       echo "🔗 Enlace directo al Summary:"
      #       echo "$URL_SUMMARY"
      #       exit 1
      #     fi
          
      #     echo ""
      #     echo "✅ **TODAS LAS VALIDACIONES PASARON**"
      #     echo "🚀 Workflow aprobado - puede continuar"
      #     echo "🚀 Procediendo con la publicación de la imagen..."

      # # ============ PUBLICACION DE IMAGEN ============
      # - name: stp-PublishDockerImage
      #   if: success()
      #   uses: docker/build-push-action@v5
      #   with:
      #     file: docker/${{ inputs.image }}/Dockerfile
      #     push: false
      #     no-cache: true
      #     tags: |
      #       ${{ inputs.acr_name }}.azurecr.io/nginx-${{ inputs.image }}:${{ env.IMAGE_TAG_SHA }}
      #       ${{ inputs.acr_name }}.azurecr.io/nginx-${{ inputs.image }}:${{ env.IMAGE_TAG_ENV }}
      #     labels: |
      #       org.opencontainers.image.source=https://github.com/${{ github.repository }}
      #       org.opencontainers.image.revision=${{ github.sha }}
      #       org.opencontainers.image.created=$(date -u +'%Y-%m-%dT%H:%M:%SZ')
      #       org.opencontainers.image.version=${{ env.IMAGE_TAG_ENV }}